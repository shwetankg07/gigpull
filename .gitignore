node_modules/
dist/
.next/
next-env.d.ts
*.tsbuildinfo

# Secrets, and GIGPULL_ANCHORS — the places you can already be on a weekday.
# Those coordinates are personal information; keep them out of the repository.
# .env.example is the tracked template and must stay empty.
.env
.env.*
!.env.example

# Lead database — contains scraped business contact details.
# Never commit: it is other people's phone numbers and email addresses.
*.db
*.db-journal
*.db-wal
*.db-shm

# Operator profile — describes you, may contain rate floors and preferences.
# profile.example.md is the tracked template.
profile.md

# Scratch files used while driving the pipeline by hand
smoke.ts
e2e.ts
osmcheck.ts
*.local.json

.DS_Store
*.log
